import { useRef, useEffect } from 'react'

export function usePinchZoom() {
    const viewRef = useRef<HTMLElement | HTMLDivElement>(null)

    useEffect(() => {
        
    }, [])

    return viewRef
}

export default usePinchZoom
