export default definePageConfig({
  navigationBarTitleText: '元素周期表PRO',
  enableShareAppMessage: true,
  enableShareTimeline: true,
  // renderer: 'skyline',
  // componentFramework: 'glass-easel',
  // disableScroll: true,
})
